ANTHROPIC_API_KEY = "YOUR_KEY_HERE"

# Optional: lock the whole app behind one password (anyone with the URL must enter it).
# Use together with Streamlit "Private" app visibility for best security.
# APP_ACCESS_PASSWORD = "choose-a-strong-password"
